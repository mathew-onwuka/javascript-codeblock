let tries = 0;
while (tries < 3) {
    console.log(tries);
    tries++
}

let count = 3;
while (count > 0) {
    console.log(count);
    count --;
}

//Assignment 1:  Difference between for loop and while loop with code examples